# EM算法

## 概述

期望最大化算法是处理含有隐变量的概率模型参数估计的经典方法。

## 数学原理

### E步：计算期望
计算隐变量的后验分布：$Q(\theta|\theta^{(t)}) = E_{Z|X,\theta^{(t)}}[\log p(X,Z|\theta)]$

### M步：最大化期望
$\theta^{(t+1)} = \arg\max_\theta Q(\theta|\theta^{(t)})$

## 应用
- 高斯混合模型
- 隐马尔可夫模型
- 缺失数据处理